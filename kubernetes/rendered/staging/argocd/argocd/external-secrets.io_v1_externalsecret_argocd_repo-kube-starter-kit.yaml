# Source: infrastructure-app-of-apps/templates/ExternalSecret.github-deploy-key.yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: repo-kube-starter-kit
  namespace: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: repo-kube-starter-kit
    creationPolicy: Owner
    template:
      engineVersion: v2
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repository
      data:
        project: default
        url: git@github.com:DevOps-Directive/kube-starter-kit.git
        type: git
        # populated from spec.data below
        sshPrivateKey: ""
  data:
    - secretKey: sshPrivateKey
      remoteRef:
        key: ksk-use2-staging-eks-deploy-key
        # Key within AWS secret
        property: sshPrivateKey
