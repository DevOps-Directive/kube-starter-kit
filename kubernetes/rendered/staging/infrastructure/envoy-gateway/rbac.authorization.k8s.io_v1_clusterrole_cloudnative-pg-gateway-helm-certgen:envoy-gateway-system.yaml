# Source: envoy-gateway-wrapper/charts/gateway-helm/templates/certgen-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cloudnative-pg-gateway-helm-certgen:envoy-gateway-system
  labels:
    helm.sh/chart: gateway-helm-1.6.0
    app.kubernetes.io/name: gateway-helm
    app.kubernetes.io/instance: cloudnative-pg
    app.kubernetes.io/version: v1.6.0
    app.kubernetes.io/managed-by: Helm
  annotations:
    helm.sh/hook: pre-install, pre-upgrade
    helm.sh/hook-weight: "-1" # Ensure rbac is created before the certgen job when using ArgoCD.
rules:
  - apiGroups:
      - admissionregistration.k8s.io
    resources:
      - mutatingwebhookconfigurations
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - admissionregistration.k8s.io
    resources:
      - mutatingwebhookconfigurations
    resourceNames:
      - envoy-gateway-topology-injector.envoy-gateway-system
    verbs:
      - update
      - patch
