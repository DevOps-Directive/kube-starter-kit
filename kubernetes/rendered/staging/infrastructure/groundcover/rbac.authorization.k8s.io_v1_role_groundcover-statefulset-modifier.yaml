# Source: groundcover-wrapper/charts/groundcover/templates/backend/statefulset-modifier/role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: groundcover-statefulset-modifier
  labels:
    app.kubernetes.io/part-of: groundcover
  annotations:
    helm.sh/hook-weight: "-1"
    helm.sh/hook: pre-upgrade
    helm.sh/hook-delete-policy: before-hook-creation
rules:
  - apiGroups:
      - ""
    resources:
      - persistentvolumeclaims
    verbs:
      - get
      - list
      - patch
  - apiGroups:
      - apps
    resources:
      - statefulsets
    verbs:
      - get
      - list
      - patch
      - delete
