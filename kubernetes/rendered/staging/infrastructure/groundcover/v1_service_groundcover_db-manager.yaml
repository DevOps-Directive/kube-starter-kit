# Source: groundcover-wrapper/charts/groundcover/templates/backend/db-manager/service.yaml
apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app.kubernetes.io/part-of: groundcover
    app: db-manager
    app.kubernetes.io/name: db-manager
  name: db-manager
  namespace: groundcover
spec:
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: http
  selector:
    app: db-manager
  type: ClusterIP
