# Source: external-dns-wrapper/charts/external-dns/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: cloudnative-pg-external-dns
  namespace: external-dns
  labels:
    helm.sh/chart: external-dns-1.19.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: cloudnative-pg
    app.kubernetes.io/version: 0.19.0
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: cloudnative-pg
  ports:
    - name: http
      port: 7979
      targetPort: http
      protocol: TCP
