apiVersion: v1
kind: Service
metadata:
  name: go-backend
  namespace: go-backend-kustomize
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: go-backend
  type: ClusterIP
