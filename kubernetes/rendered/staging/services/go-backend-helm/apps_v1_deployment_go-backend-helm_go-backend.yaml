# Source: minimal/templates/Deployment.go-backend.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-backend
  namespace: go-backend-helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-backend
  template:
    metadata:
      labels:
        app: go-backend
        # annotations:
        #   instrumentation.opentelemetry.io/inject-go: "honeycomb/otel-autoinstrumentation"
        #   # Go Requires an additional annotation to the path of the binary on the container
        #   instrumentation.opentelemetry.io/otel-go-auto-target-exe: "/bin/server"
    spec:
      containers:
        - name: go-backend
          image: 857059614049.dkr.ecr.us-east-2.amazonaws.com/services/go-backend:0.2.2-rc0000-g808175d
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: go-backend-pg-app
                  key: uri
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              # The docs tell you to ship directly to signoz cloud... but then every pod needs the api key 🤔
              value: signoz-k8s-infra-otel-agent.signoz:4317
            - name: OTEL_SERVICE_NAME
              value: go-backend-helm
