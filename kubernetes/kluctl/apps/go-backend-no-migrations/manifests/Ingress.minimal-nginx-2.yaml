apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-nginx-2
  namespace: apps
  annotations:
    external-dns.alpha.kubernetes.io/hostname: ingress-nginx.staging.kubestarterkit.com
spec:
  ingressClassName: nginx
  rules:
    - host: "ingress-nginx-2.staging.kubestarterkit.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-backend-no-migrations
                port:
                  number: 8080
