See ./mise.toml for info about how manifests were generate with istioctl

TODO: 
- Should this just use kustomize? (instead of being a helm chart?)
- Need a way to specify versions per environment (since each could have its own generated manifests)
