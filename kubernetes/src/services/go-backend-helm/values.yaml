db:
  # TODO: make cnpg optional
  image: ghcr.io/cloudnative-pg/postgresql:17.7-minimal-trixie
  storageClass: ebs-gp3-encrypted
replicas: 1
image:
  version: 6956bdb-0000-g6956bdb # staging_services/go-backend
ingress:
  traefik:
    hostname: ingress-traefik-helm.staging.kubestarterkit.com
    clusterIssuer: letsencrypt-prod-dns
    # tlsSecretName: optional, defaults to hostname with dots replaced by dashes + "-tls"
gateway:
  traefik:
    hostname: gateway-traefik-helm.staging.kubestarterkit.com
