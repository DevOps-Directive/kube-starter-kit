apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: go-backend-pg
spec:
  instances: 1
  storage:
    size: 1Gi
    storageClass: OVERRIDE_ME
  superuserSecret:
    name: go-backend-db-superuser
  imageName: dhi.io/postgres:17-alpine3.22
