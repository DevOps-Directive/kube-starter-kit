apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: deploy-key
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: deploy-key
  dataFrom:
    - extract:
        key: staging-us-east-2-deploy-key
