stack {
  id          = "staging-gbl-bootstrapping"
  name        = "bootstrapping"
  description = "Account bootstrapping for staging - IAM role and Route53 zone"
  tags        = ["staging", "global", "bootstrapping"]
}
