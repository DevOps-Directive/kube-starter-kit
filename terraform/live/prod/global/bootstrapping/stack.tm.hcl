stack {
  id          = "example-prod-gbl-bootstrapping"
  name        = "bootstrapping"
  description = "Account bootstrapping for production (example)"
  tags        = ["prod", "global", "bootstrapping", "infrastructure", "example"]
}
