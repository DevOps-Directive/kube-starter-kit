stack {
  id          = "example-prod-gbl-bootstrapping"
  name        = "bootstrapping"
  description = "Account bootstrapping for production"
  tags        = ["prod", "global", "bootstrapping", "infrastructure"]
}
